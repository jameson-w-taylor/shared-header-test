import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'shared-header-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
